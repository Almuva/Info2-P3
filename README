Para commitear uso:
add lo_que_sea (((si es un archivo nuevo O SI SE HA MODIFICADO!!)))
(((git status para ver estado)))
git commit -m "comentario"
git push origin master (((Se actualiza la página web...)))


---------

mola: para hacer un add de los archivos modificados ya añadidos, usar [  git add -u  ]

---------

Resolver conflictos cuando no deja hacer push:
--git fetch origin; git merge origin/master (((hace un merge automático)))

---------

COMANDOS DE GIT CON EJEMPLOS!!--> http://sysmonblog.co.uk/misc/git_by_example/
